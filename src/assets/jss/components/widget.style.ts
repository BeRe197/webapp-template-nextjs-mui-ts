import {container, btnAnimation} from "@/assets/jss/global.style";

const widgetStyle = {
    container: {
        ...container,
    },
    btnAnimation: {
        ...btnAnimation,
    },
    item: {
        mt: "2rem",
    },
};
export default widgetStyle;
